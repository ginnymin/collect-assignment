const breakpoints = {
    sm: 480,
    md: 800,
    lg: 1200
};

const grid = {
    itemWidth: 328,
    imageHeight: 220,
    imageRatio: 220 / 328
};

const colors = {
    text: '#222222',
    textLight: '#9a9a9a',
    primary: '#9f0086',
    gray: '#eeeeee',
    disabled: '#cccccc',
    error: '#F44336'
};

const config = {
    breakpoints,
    colors,
    grid
};

export default config;
